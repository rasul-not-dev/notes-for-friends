🔹1 --- создаем модель в файле models.py 

from django.db import models
class Artiles(models.Model):
    title = models.CharField('название')
    
	def __str__(self): 
        return self.title
    
    class Meta:
        verbose_name = 'новость'
        verbose_name_plural = 'новости'

🔹2 --- не забываем про миграции python manage.py makemigrations/minrate

🔹3 --- в файле admin.py импортируем нашу модель from .models import Artiles
	и регистрируем admin.site.register(Artiles)

🔹4 --- создаем файл forms.py, импортируем модель и классы для создания форм: 

from django.forms import ModelForm, TextInput, DateTimeInput, Textarea

class ArtilesForm(ModelForm): --- называем класс почти как модель
    class Meta:
        model = Artiles
        fields = \['title', 'anons', 'full_text', 'date']

для стилизации в классе meta создаем список widgets

widgets = {
            'title': TextInput(attrs={
                'class': 'input-field',
                'placeholder': 'Название статьи',
            }),
            'anons': TextInput(attrs={
                'class': 'input-field',
                'placeholder': 'Краткий анонс',
            }),
            'full_text': Textarea(attrs={
                'class': 'input-field input-field--textarea',
                'placeholder': 'Текст статьи',
                'rows': 6,
            }),
            'date': DateTimeInput(attrs={
                'class': 'input-field',
                'type': 'datetime-local',
                'placeholder': 'Дата и время',
            }),
        }

🔹5 --- работаем со views.py
импортируем классы и создаем обьекты

from django.shortcuts import render
from .models import Artiles
from .forms import ArtilesForm


def news_home(request):
    news = Artiles.objects.order_by('-date')
    return render(request, 'news/news_home.html', {'news': news})

def create(request):
    form = ArtilesForm()
    return render(request, 'news/create.html', { 'form': form})

🔹6 --- наша кнопка с типом submit собирает все с нашей формы и отправляем post запрос
поэтому прописываем следующее во views.py 

from django.shortcuts import render, redirect
def create(request):
    error = ''
    if request.method == 'POST': ---  post применяем к форме \<form method="post">
        form = ArtilesForm(request.POST)
        if form.is_valid():
            form.save()
            return redirect('news_home') --- редиректимся на страницу home
        else:
            error = 'неверная форма'
    
    form = ArtilesForm()
    return render(request, 'news/create.html', { 'form': form, 'error': error})