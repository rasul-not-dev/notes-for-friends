## Другие методы отображения

|Метод|Что делает|
|---|---|
|`{{ form.as_p }}`|Все поля в `<p>`|
|`{{ form.as_table }}`|Все поля в таблице (`<tr>`)|
|`{{ form.as_ul }}`|Все поля в списке (`<li>`)|
|`{{ form }}`|Без обёртки (голые поля)|
