
https://www.youtube.com/watch?v=ajxM-EaLezc

| **Категория**        | **Технология / Библиотека**     | **Что именно нужно уметь делать**                                                                                                                                                  |
| -------------------- | ------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Язык & База**      | **Продвинутый Python**          | ООП (классы, наследование), работа с `*args` и `**kwargs`, метод `super()`, декораторы.                                                                                            |
| **Веб-фреймворк**    | **Django (Monolith)**           | Создание приложений, кастомные методы моделей (переопределение `save()`), связи `ForeignKey`, сессии и контекст-процессоры (для корзины), встроенная аутентификация пользователей. |
| **Базы данных**      | **PostgreSQL & pgAdmin**        | Подключение Postgres к Django (через `psycopg2`), работа с миграциями, базовое понимание реляционных БД.                                                                           |
| **Контейнеризация**  | **Docker & Docker Compose**     | Написание `Dockerfile` и `docker-compose.yml`, сборка и запуск Django + Postgres в изолированных контейнерах командами вроде `docker compose up --build`.                          |
| **Безопасность**     | **Переменные окружения (.env)** | Скрытие секретных ключей, паролей от БД и API-ключей с помощью библиотек вроде `django-environ` или `python-dotenv`.                                                               |
| **Интеграции (API)** | **Stripe API & Webhooks**       | Работа с платежным шлюзом Stripe: отправка пользователя на оплату и обработка ответов от Stripe через вебхуки (`webhooks`), чтобы подтвердить заказ в базе данных.                 |
| **Деплой & Сервер**  | **Linux (Ubuntu), Git, Vim**    | Подключение к удаленному серверу по SSH, клонирование репозитория через Git, базовое редактирование файлов прямо в консоли через редактор Vim.                                     |